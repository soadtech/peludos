import React from 'react';
import { View, Text, SafeAreaView } from 'react-native'

const Home = () => {
    return (
        <SafeAreaView>
            <Text>Homee</Text>
        </SafeAreaView>
    );
}

export default Home;